#define _CRT_SECURE_NO_WARNINGS 1 

#include <stdio.h>

int main()
{
	int arr[10] = { 0 };
	printf("%p\n", arr);
	printf("%p\n", arr + 1);
	printf("%p\n", &arr[0]);//数组名为其首元素地址
	printf("%p\n", &arr[0] + 1);
	printf("%p\n", &arr);//整个数组的地址即为首元素地址
	printf("%p\n", &arr + 1);
	return 0;
}
