#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	int i = 1;
	printf("1到100的奇数有：");
	while (i <= 100)//用while循环
	{
		printf("%d ", i);//1加2得下一个奇数
		i += 2;
		//if (i % 2 == 1)
		//	printf("%d ", i);
		//i++;
	}
	//for (i = 1; i <= 100; i++)//用for循环
	//{
	//	int num = i % 2;
	//	if (1 == num)
	//		printf("%d ", i);
	//}
	return 0;
}
