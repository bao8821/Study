#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	int n,i;
	int sum = 1;
	printf("请输入待计算数字：");
	scanf("%d", &n);
	for (i = 1; i <= n; i++)
		sum = sum * i;
	printf("%d的阶乘为%d.", n,sum);
}
